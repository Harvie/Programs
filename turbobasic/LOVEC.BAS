LET N = 5
LET G = 10
LET NALEZ = 0
LET I = 0
LET A = INT(G * RND)
LET B = INT(G * RND)
OLAL:

IF NOT ( NALEZ = 0 AND I <= 5) THEN GOTO FEDO
INPUT "X,Y" ;X,Y
IF ABS (X-A)+ABS(Y-B)=0 THEN GOTO FEDOS
GOSUB FEDOE
GOTO OLA
FEDOS:
PRINT "ZVIRE NALEZENO:";A,B
LET NALEZ = 1
OLA :
LET I = I+1
GOTO OLAL
END
FEDOE:
PRINT "JDI NA ";
IF X < A
IF X > A
IF X <> A
IF
IF
RETURN
